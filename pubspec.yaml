name: bustime_flutter
description: A new Flutter application.

version: 2.0.1+13

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  http: ^0.12.0+4
  provider: ^4.0.2
  firebase_admob: ^0.9.0+10

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true


  assets:
    - images/